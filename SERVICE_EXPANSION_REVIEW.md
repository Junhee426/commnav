# 서비스 확장 검토 (Service Expansion Review)

이 저장소는 백엔드가 없는 순수 클라이언트 사이드 도구이므로, 여기서 "서비스 확장"은 배포·트래픽 확장이 아니라 **README의 "다음 확장 지점"에 나열된 로드맵(공식 KPS 궤도·실제 GNSS 궤도력 입력, 항법 신호/수신기 오차모델, 도플러/반송파 시간연속 추정, 위성별 상관 오차, 추가 탑재 자원 예산, 시간변화 간섭·강우, 트래픽/핸드오버 모델)을 지금 구조 위에 이어붙일 수 있는가**를 뜻합니다. 이번 검토는 코드를 읽고, 구조적 마찰이 실제로 있는 지점을 찾아 작게 고치는 데 집중했습니다.

## 1. 구조 진단

- **위성군 생성이 로드맵의 핵심 확장 지점인데 하드코딩되어 있었습니다.** `dist/engine.js`의 `buildConstellations()` 하나에 사용자 설정 LEO 위성군, 고정 GNSS 기준군(24기), 고정 지역항법 예시(GEO 3 + IGSO 5)가 각각 별도의 `for` 루프와 매직 넘버(6, 4, 20200, 55, 0.35, 120, 8, 43, 128 …)로 함께 박혀 있었습니다. "공식 KPS 궤도 입력"이나 "실제 GNSS 궤도력 입력" 같은 로드맵 항목을 넣으려면 이 함수 본문을 직접 수정해야 했고, 새 위성군 유형을 하나 추가하려면 기존 루프를 복사해 변형하는 것이 유일한 방법이었습니다.
- **위성 그룹 이름(`'LEO'`/`'GNSS'`/`'REGIONAL'`)은 `engine.js`, `rendering.js` 여러 곳에 문자열로 흩어져 있습니다.** 이는 지금 당장 문제는 아니지만(그룹 판별 로직 자체는 일반적으로 짜여 있음—`positionAccuracy`는 그룹 집합을 동적으로 계산합니다), 완전히 새로운 위성군 종류(예: 별도의 지역 증강 시스템)를 추가하면 `rendering.js`의 색상·마커 분기에도 손이 가야 함을 의미합니다. 이번 범위에서는 손대지 않았고, 아래 "이번에 하지 않은 것"에 기록해 둡니다.
- **오차·통신 모델은 이미 한 곳에 모여 있어 확장에 유리합니다.** 측정 잡음(`sat.sigma` 계산), 위치정확도 최소자승(`positionAccuracy`), 링크버짓(`linkBudget`)이 각각 `evaluateSnapshot`/`positionAccuracy`/`linkBudget` 함수 하나에만 있어, "항법 신호/수신기 오차모델"이나 "시간변화 간섭·강우" 항목을 추가할 때 건드릴 지점이 명확합니다. 다만 `positionAccuracy`의 가중치는 대각 공분산(스칼라 `sigma`)만 가정하므로, "위성별 상관된 궤도·시계오차"를 넣으려면 이 함수의 정규방정식 구성 방식(현재는 `weight = 1/sigma²`로 대각 가중만 지원) 자체를 확장해야 합니다 — 이는 이번 범위 밖의 실제 로드맵 작업입니다.
- **24시간 연산은 이미 Web Worker(`dist/analysis-worker.js`)에서 실행되어 메인 스레드를 막지 않습니다.** 288개 표본 × 최대 544기(LEO 512 + GNSS 24 + 지역 8) 정도의 반복도 워커 안에서만 발생하고, `orbitState`의 `raan`/`inclination` 코사인·사인을 위성군 생성 시 1회만 계산해 두는 등 GC 압박을 줄이려는 주석 섞인 최적화가 이미 되어 있었습니다. 512기 상한(`validateConfig`)과 스윕 축의 "512 초과 시 unavailable 처리"(`parameterSweep`)도 이미 방어되어 있어 별도 수정이 필요하지 않았습니다.

## 2. 이번에 변경한 것

1. **`dist/engine.js`: 위성군 생성을 재사용 가능한 생성기 + 데이터 스펙으로 분리.**
   - `walkerDelta(spec)`를 새 함수로 추출했습니다. Walker Delta(F=1) 패턴 수식(궤도면 RAAN 배치, 면당 위상 배치, 면간 위상 오프셋)을 한 곳에만 두고, 사용자 LEO 위성군과 고정 GNSS 기준군이 이 함수를 서로 다른 스펙으로 호출하도록 바꿨습니다.
   - 고정 GNSS 기준군의 수치를 `GNSS_REFERENCE` 상수 객체로, 고정 지역항법 예시(GEO/IGSO)의 수치를 `REGIONAL_REFERENCE` 상수 객체로 뽑아냈습니다. 이제 "실제 GNSS 궤도력" 또는 "공식 KPS 설계궤도"로 교체하거나 다른 기준군을 하나 더 추가하는 작업은, 이 두 함수(`buildConstellations`, `walkerDelta`) 본문을 고치는 게 아니라 스펙 객체 하나를 새로 쓰는 일이 됩니다.
   - **수치 결과는 완전히 동일합니다.** 위성 ID, RAAN, 위상, 반지름, 경사각 공식을 한 항도 바꾸지 않고 그대로 옮겼으며, 기존 `tests/engine.test.js`의 위성 수·기하학 검증(예: `orbits[0].radius`, `R1` ID, LEO/GNSS/REGIONAL 개수)이 수정 없이 그대로 통과합니다.
   - 검증용 테스트 2건을 추가했습니다: `walkerDelta`가 사용자 LEO 위성군과 `GNSS_REFERENCE`로 만든 결과가 `buildConstellations`의 실제 출력과 일치하는지, 그리고 `REGIONAL_REFERENCE`가 실제 데이터 스펙으로 동작하는지(개수·ID·경사각이 스펙 값에서 유도됨)를 확인합니다.

2. **`dist/app.js`: CSV 내보내기에서 동일한 시나리오 JSON을 표본마다 다시 직렬화하던 부분 수정.**
   - `downloadCSV()`가 288개(또는 향후 표본 수가 더 늘어날 경우 더 많은) 행마다 완전히 동일한 `used`(실행 당시 설정) 객체를 `JSON.stringify`로 매번 다시 문자열화하고 있었습니다. 값은 모든 행에서 동일하므로 루프 밖에서 한 번만 직렬화하도록 고쳤습니다. 기능은 동일하고 불필요한 반복 작업만 제거한 것으로, 로드맵의 "더 세밀한 시간연속 추정/샘플링"처럼 표본 수가 늘어나는 방향과 맞물려 실제로 의미가 있는 사소하지만 확실한 수정입니다.
   - 이 함수는 이전까지 테스트가 전혀 없었습니다(빌드/DOM/Worker를 흉내 낸 `tests/app.test.js`에 CSV 내려받기 자체를 실행하는 테스트가 없었음). 이번에 Node의 전역 `Blob`/`URL.createObjectURL`을 테스트 하네스에 연결하여, 내보낸 CSV가 헤더 1행 + 표본당 1행을 갖고 각 행의 `scenario_config_json`이 실제 실행에 쓰인 설정과 일치하는지 확인하는 테스트를 새로 추가했습니다.

## 3. 검증

- `npm run build` (정적 파일·상대경로·JS 문법 검사 + 전체 테스트) 통과: 39개 테스트 모두 성공(`# pass 39`, `# fail 0`).
- `npm test` 동일하게 통과.
- 위성군 관련 기존 테스트(개수·ID·궤도반지름·정지궤도 고정성 등)가 리팩터링 후에도 수정 없이 통과하여, 수치 결과가 리팩터링 전후로 동일함을 확인했습니다.

## 4. 이번에 하지 않은 것 (의도적으로 범위 밖)

- README 로드맵의 실제 항목들(공식 KPS 궤도·실제 GNSS 궤도력 입력, 항법 신호/수신기 오차모델, 도플러/반송파 시간연속 추정, 위성별 상관 오차, 추가 탑재 자원 예산, 시간변화 간섭·강우, 트래픽/핸드오버 모델)은 이번 범위에서 구현하지 않았습니다. 이번 변경은 그 항목들을 "구조적으로 더 붙이기 쉽게" 만드는 것까지만입니다.
- `positionAccuracy`의 대각 가중(스칼라 `sigma`) 가정을 상관 오차(공분산 비대각 성분)까지 받도록 일반화하는 작업은 하지 않았습니다. 실제 로드맵 항목("위성별 상관된 궤도·시계오차")이며, 최소자승 정규방정식 구성 자체를 바꿔야 하는 더 큰 작업입니다.
- 위성 그룹 이름(`'LEO'`/`'GNSS'`/`'REGIONAL'`)이 `rendering.js`에도 문자열로 남아 있는 부분은 그대로 두었습니다. 지금은 실질적 마찰이 없고(그룹 판별 로직 자체는 일반적), 억지로 테이블화하면 오히려 시각화 스타일(색상·마커 모양)이라는 별개 관심사를 뒤섞을 위험이 있다고 판단했습니다.
- 메인 스레드에서 매 프레임(재생 애니메이션, 350ms 간격) 실행되는 단일 시각(snapshot) 계산은 현재 위성 수 상한(512+24+8기)에서 성능 문제가 관측되지 않아 그대로 두었습니다. 24시간 전체 계산은 이미 Worker에서만 실행됩니다.
